<controls:MetroWindow x:Class="SpecLogLogoReplacer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="SpecLog Logo Replacer" Height="350" Width="525"
        DataContext="{Binding Main, Source={StaticResource Locator}}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <Label>Path to exported html file:</Label>
    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=PathToSpecLogFile}" />
    <Label Grid.Row="1" Grid.Column="0">Path to logo of your company:</Label>
    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Path=PathToLogo}" />
    <Label Grid.Row="2" Grid.Column="0">Supported image types: .png</Label>
    <Button Grid.Row="2" Command="{Binding Path=Transform}" Grid.Column="1" Height="Auto" Width="Auto" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10">Transform!</Button>
  </Grid>
</controls:MetroWindow>
