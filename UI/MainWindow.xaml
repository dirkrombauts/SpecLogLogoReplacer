<controls:MetroWindow x:Class="Aim.SpecLogLogoReplacer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="SpecLog Logo Replacer" Height="480" Width="640"
        DataContext="{Binding Main, Source={StaticResource Locator}}"
                      Loaded="MainWindow_OnLoaded" Closed="MainWindow_OnClosed">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="20" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="10" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="20" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="20" />
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition />
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="20" />
    </Grid.ColumnDefinitions>

    <Label Grid.Row="1" Grid.Column="1">Path to exported HTML file:</Label>
    <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding Path=PathToSpecLogFile}" />
    <Button Grid.Row="1" Grid.Column="3" Command="{Binding Path=BrowseForSpecLog}">...</Button>
    
    <Label Grid.Row="3" Grid.Column="1">Path to new logo:</Label>
    <TextBox Grid.Row="3" Grid.Column="2" Text="{Binding Path=PathToLogo}" />
    <Button Grid.Row="3" Grid.Column="3" Command="{Binding Path=BrowseForLogo}">...</Button>
    <Label Grid.Row="4" Grid.Column="2">Supported image types: .png</Label>
        <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="3" TextWrapping="Wrap">SpecLog Logo Replacer is licensed under a BSD license. Neither AIM Software GmbH nor SpecLogLogoReplacer are endorsed by or affiliated with SpecLog, SpecFlow or TechTalk in any way.</TextBlock>

        <StackPanel Grid.Row="6" Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,0,12">
      <Label>Provided by:</Label>
      <Button Cursor="Hand" Command="{Binding Path=NavigateToAim}">
        <Button.Template>
          <ControlTemplate TargetType="Button">
            <Image Height="40" Source="./Resources/aim.png"></Image>
          </ControlTemplate>
        </Button.Template>
        </Button>
    </StackPanel>
    
    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Path=Feedback}" TextWrapping="Wrap" Margin="5" />
    
    <Button Grid.Row="5" Command="{Binding Path=Transform}" Grid.Column="2" Grid.ColumnSpan="1" Height="Auto" Width="Auto" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,10">Transform!</Button>
  </Grid>
</controls:MetroWindow>
